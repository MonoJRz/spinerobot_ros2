# spinerobot_interfaces

ROS 2 messages, services, and actions for the spine robot. Build this package
before consumers such as `spinerobot_tracking`. `TrackingStatus` reports
per-marker visibility, validity, age, quality, reason, and acquisition time.
